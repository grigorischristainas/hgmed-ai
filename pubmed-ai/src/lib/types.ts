export type PubMedResult = {
    id: string
    title: string
    abstract: string
    publicationDate: string
    authors: string[]
}

export type PaperSummary = {
    intervention: string
    disease: string
    effectiveness: string
}
